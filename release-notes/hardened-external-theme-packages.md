---
category: security
audience: users, operators
area: themes
action: none
breaking: false
---
Installed theme packages can no longer run script through an SVG asset or read files outside their own directory, and archives built with `git archive` or the default macOS `tar` now install instead of failing.
