---
category: added
audience: operators
area: packaging
action: none
breaking: false
---
Every update to the main branch now publishes a container image to the repository's GitHub Packages registry, so an instance can be pulled from `ghcr.io` without building it locally.
